export const SERVER_IP = "192.168.0.222";
export const FRONTEND_PORT = 5173;
export const BACKEND_PORT = 3001;

export const FRONTEND_URL = `http://${SERVER_IP}:${FRONTEND_PORT}`;
export const BACKEND_URL = `http://${SERVER_IP}:${BACKEND_PORT}`;
export const MOBILE_URL = `${FRONTEND_URL}/mobile`;

// Must match backend CANVAS_WIDTH / CANVAS_HEIGHT
export const GAME_WIDTH = 1600;
export const GAME_HEIGHT = 800;
